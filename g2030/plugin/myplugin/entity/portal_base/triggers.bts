ENTITY_CLICK
{
Test("1111111111")
}

ENTITY_TOUCH_ALL{

    If(IsPlayer(entity = $obj2))
    {
    $targetPos = GetConfigVar(obj = $obj1,key="toPos")
    $toIslandId = GetConfigVar(obj = $obj1,key="toIslandId")
    PortalUIData(player = $obj2, pos=$targetPos,toIsland=$toIslandId)
    }
}